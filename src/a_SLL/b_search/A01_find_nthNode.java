package a_SLL.b_search;

/**
 * ********************************************************************************
 * Write a function to get Nth node in a Linked List
 * ********************************************************************************
 */
public class A01_find_nthNode {
	/*
	 * traverse the list (maintain a count)
	 * 
	 * at each node do count++
	 * if (count==n0)
	 * 	 break
	 * otherwise
	 *   move to next node
	 * 
	 */
}
