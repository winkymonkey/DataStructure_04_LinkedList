package a_SLL.b_search;

/**
 * ********************************************************************************
 * Nth node from the end of a Linked List
 * ********************************************************************************
 */
public class A04_find_nthNodeFromEnd {
	/*
	 * traverse the list (use two pointers p1, p2)
	 * p1,p2 will start from first node
	 * 
	 * p1 will traverse 'n' nodes alone and p2 will stay idle
	 * 
	 * p1 will increment as p1+=1
	 * p2 will increment as p1+=2
	 * 
	 * So when p1 will reach last node, p2 will remain at nth node from end
	 * 
	 */
}
