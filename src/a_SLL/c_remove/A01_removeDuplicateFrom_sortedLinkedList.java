package a_SLL.c_remove;

/**
 * ********************************************************************************
 * Remove duplicates from a sorted linked list
 * ********************************************************************************
 */
public class A01_removeDuplicateFrom_sortedLinkedList {
	/*
	 * traverse the list
	 * if(node.data == node.next.data)
	 * 		//remove next node
	 * else
	 * 		node = node.next;
	 * 
	 */
}
