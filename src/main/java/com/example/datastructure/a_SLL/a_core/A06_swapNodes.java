package com.example.datastructure.a_SLL.a_core;

/**
 * ********************************************************************************
 * Swap nodes in a linked list without swapping data
 * ********************************************************************************
 */

public class A06_swapNodes {
	/*
	 * Suppose we need to swap C and H
	 * 
	 * A -> B -> C -> D -> E -> F -> G -> H -> I -> J 
	 *           ^                        ^
	 *           |                        |
	 *           T1                       T2
	 * 
	 * Traverse the list and create two dummy nodes T1 and T2 
	 * in such a way that "T1.next = C node" and "T2.next = H node"
	 * 
	 * 
	 * Now apply deletion and insertion logic at those two places
	 * 
	 */
}
