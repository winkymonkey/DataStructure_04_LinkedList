package com.example.datastructure.a_SLL.z_misc1;

/**
 * ********************************************************************************
 * Detect loop in a linked list
 * ********************************************************************************
 */
public class A01_detectLoop {
	/*
	 * traverse the list
	 * in each iteration mark each node as visited
	 * if(node.next.visited==true)
	 * 	 there is a loop
	 * else
	 * 	 node = node.next
	 * 
	 */
}
