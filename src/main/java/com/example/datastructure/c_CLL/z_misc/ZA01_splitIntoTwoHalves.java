package com.example.datastructure.c_CLL.z_misc;

/**
 * *****************************************************************************
 * Split a Circular Linked List into two halves
 * *****************************************************************************
 */

public class ZA01_splitIntoTwoHalves {
	/*
	 * Traverse the CLL using 2 pointers.
	 * In each iteration, slow pointer increment by 1, whereas fast pointer increments by 2
	 * So, when slow pointer will be at middle, fast pointer will be at end
	 * Store the mid and last pointers of the CLL
	 * 
	 * Make the second half circular.
	 * Make the first half circular.
	 * Set head (or start) pointers of the two linked lists.
	 * 
	 */
}
